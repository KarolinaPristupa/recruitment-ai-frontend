import React from 'react';
import styles from './index.module.scss';

const Logo: React.FC = () => {
  return (
    <div className={styles.logo}>
      <span>V.AI</span>
    </div>
  );
};

export default Logo;
